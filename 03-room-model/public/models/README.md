# models/

講師から提供される教室モデルをここに配置してください。

```
models/
├── room.obj
├── room.mtl
└── textures/
```

このリポジトリにはモデルファイル自体は含まれていません（別途配布）。
配置しない状態で `03-room-model` を起動すると、`app.js` のフォールバック処理により
簡易的なグレーのBoxが代わりに表示されます。
